[package]
name = "hyprwire"
version = "0.0.0"
edition = "2024"
description = "A fast and consistent wire protocol for IPC"
license = "BSD-3-Clause"

[dependencies]
env_logger = "0.11.9"
log = "0.4.29"
nix = { version = "0.31.1", features = ["poll", "socket", "uio"] }
